{% extends 'base.html.twig' %}
{% block body %}

  
   <h2>Liste des Courses</h2>
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th></th>
                  
                   <th></th>
                </tr>
              </thead>
              <tbody>
               {% for Course in Courses %}
               <tr>
              <td>{{Course.libCourse}}</td>
             
              
              <td><a class="nav-link" href="{{ path('participant_course', {'id': Course.getNumCourse()}) }}">Details de la Course</a></td>
               {% endfor %}
              </tbody>
            </table>
          </div>

{% endblock %}
              


             